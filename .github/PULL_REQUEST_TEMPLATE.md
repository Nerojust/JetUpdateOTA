## What does this change do?

<!-- One or two sentences. Link an issue if there is one. -->

## Why

<!-- The motivation. Skip this if the issue link already covers it. -->

## Testing

<!-- What did you run, and what did it show? -->

- [ ] `./gradlew test` passes (if you touched Android code)
- [ ] `./gradlew ktlintCheck` passes (if you touched Kotlin)
- [ ] `npm test` passes in `backend-reference/` (if you touched the backend)
- [ ] `npm run lint` passes in `backend-reference/` (if you touched the backend)
- [ ] New behavior has a new test demonstrating it

## Checklist

- [ ] This doesn't change the public API surface, OR I opened an issue
      to discuss the change first (link it above)
- [ ] I updated `CHANGELOG.md` if this is user-visible
